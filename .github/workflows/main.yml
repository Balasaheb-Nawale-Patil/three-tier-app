name: Three Tier CI

on:
 push:
   branches: [ main ]

jobs:
 build:
   runs-on: ubuntu-latest

   steps:

   - name: Checkout Code
     uses: actions/checkout@v3

   - name: Build Containers
     run: docker-compose build
